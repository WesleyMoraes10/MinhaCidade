<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minhacidade">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinhaCidade">
        <activity
            android:name=".activity.CadastraSugestaoActivity"
            android:parentActivityName=".activity.PrincipalMunicipeActivity"></activity>
        <activity
            android:name=".activity.CadastrarReclamacaoActivity"
            android:parentActivityName=".activity.PrincipalMunicipeActivity"></activity>
        <activity
            android:name=".activity.CadastraDenunciaActivity"
            android:parentActivityName=".activity.PrincipalMunicipeActivity" />
        <activity
            android:name=".activity.ConfiguracaoMunicipeActivity"
            android:parentActivityName=".activity.PrincipalMunicipeActivity" />
        <activity android:name=".activity.PrincipalMunicipeActivity" />
        <activity android:name=".activity.CadastroMunicipeActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>